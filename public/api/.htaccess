# Block direct HTTP access to sensitive files
<FilesMatch "\.(json|pdf)$">
    Require all denied
</FilesMatch>

# Block direct access to db.php and config.php
<FilesMatch "^(db|config)\.php$">
    Require all denied
</FilesMatch>
